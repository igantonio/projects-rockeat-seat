module.exports ={
    presets: [
        "@babel/preset-env",//Responsavel por alterar as funcionalidades do javascript que o navegador não entende
        "@babel/preset-react"//Ele vai transformar as coisas que o navegador não entende do react
    ],
}